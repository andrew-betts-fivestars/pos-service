export declare const Formatter: {
    MILLISECONDS_TO_SECONDS_DIVISOR: number;
    formatDate: (date: number) => string;
    formatMoneyString: (amount: number) => string;
    formatMstoSecs: (milliseconds: number) => number;
    formatPhone: (phone: string) => string;
    formatPlural: (quantity: number, name: string) => string;
    formatToFixedPrecision: (value: number | null, decimalPlaces: number) => number | null;
};
