import { MessageDetails } from '@fivestars-pos-sdk/types';
export declare type UnacknowledgedMessage = {
    uuid: string | null;
    messageDetails: MessageDetails;
    totalEvaluationCount: number;
    currentEvaluationCount: number;
};
declare type MessengerState = {
    pairedMessageQueue: Set<UnacknowledgedMessage>;
    resendLastPairedMessage: boolean;
};
export declare const state: MessengerState;
/**
 * Creates the EventManager (redux-saga channel)
 */
export declare const initializeMessengerState: () => Generator<never, void, unknown>;
export {};
