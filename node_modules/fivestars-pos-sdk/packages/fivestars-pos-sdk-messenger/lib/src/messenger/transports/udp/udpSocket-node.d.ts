import { UdpEvent } from '@fivestars-pos-sdk/types';
export declare const LOG_PREFIX = "UdpSocket (node)";
export declare class UdpSocket {
    static DEFAULT_PORT: number;
    static IS_BROADCAST: boolean;
    static BROADCAST_IP: string;
    server: any;
    constructor();
    open(port: number): Promise<void>;
    listen(onMessage: (updEvent: UdpEvent) => void, onError: (errorMessage: string) => void): void;
    send(message: string, address: string, port: number): Promise<void>;
    close(): Promise<void>;
}
