import { DgramModule, UdpEvent, InstoreWindow } from '@fivestars-pos-sdk/types';
declare type onMessage = (updEvent: UdpEvent) => void;
export declare const LOG_PREFIX = "UdpSocket (browser)";
export declare class UdpSocket {
    private plugin;
    static DEFAULT_PORT: number;
    static IS_BROADCAST: boolean;
    static BROADCAST_IP: string;
    static instoreWindow: InstoreWindow;
    constructor(plugin?: DgramModule.Dgram);
    open(port: number): Promise<void>;
    listen(onMessage: onMessage, onError: (errorMessage: string) => void): void;
    send(message: string, address: string, port: number): Promise<void>;
    close(): Promise<void>;
}
export {};
