import { ILogin } from '@fivestars-pos-sdk/types';
export declare const LOG_PREFIX = "Auth";
export declare class Auth {
    private core;
    constructor();
    login(info: ILogin): Promise<boolean>;
}
