/// <reference types="node" />
import { EventEmitter } from 'events';
import { WebRtcTransport, WebSocketTransport, UdpTransport } from '@fivestars-pos-sdk/fivestars-pos-sdk-messenger';
import { ILoyaltyCredentials, MessageDetails, PairedDevice, SendOptions } from '@fivestars-pos-sdk/types';
export declare class Core extends EventEmitter {
    webSocketTransport: WebSocketTransport | null;
    webRtcTransport: WebRtcTransport | null;
    udpTransport: UdpTransport | null;
    private messenger;
    constructor();
    start(loyaltyCredentials: ILoyaltyCredentials): void;
    setPairedDevice(pairedDevice: PairedDevice | null): void;
    sendPaired(messageDetails: MessageDetails, options: SendOptions): Promise<string | null>;
    startTransaction(): void;
    abortTransaction(): void;
    onMessage: (event: any) => void;
}
