import * as moment from 'moment';
export interface PairedDevice {
    apiKey: string;
    apiSecret: string;
    bluetoothMacAddress: string;
    udpPort: number;
    deviceDescription: string;
    hardwareId: string | null;
}
export declare function apiKeyToString(pairedDevice: PairedDevice): string;
export interface PairingSuccessfulMessage {
    apiKey: string;
    pairedDevice: PairedDevice;
}
export interface UnpairingSuccessfulMessage {
    apiKey: string;
}
export interface KnownDevice {
    lastUpdateReceived: moment.Moment | null;
    apiKey: string;
    apiSecret: string;
    bluetoothMacAddress: string;
    udpPort: number;
    color: string;
    deviceDescription: string;
    pairedDevice: PairedDevice | null;
    hardwareId: string | null;
}
export interface PairingState {
    deviceDescription: string | null;
    inPairingMode: boolean;
    knownDevices: KnownDevice[];
    pairedDevice: PairedDevice | null;
}
