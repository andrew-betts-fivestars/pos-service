import * as transactionTypes from './transactions';
import { PaymentTypes } from './utils/enums';

export type StartPaymentOptions = {
    amount: number;
    paymentType: PaymentTypes;
    transactionTotalUid: string | null;
    options?: Partial<transactionTypes.ITransactionOptions>;
};

export type StartRefundOptions = {
    amount: number;
    transactionTotalUid: string | null;
    isManualTransaction?: boolean;
};
