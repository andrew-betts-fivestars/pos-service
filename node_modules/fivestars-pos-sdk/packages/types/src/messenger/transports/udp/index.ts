import { MessageResult } from '../../../instore-plugins'

export const LISTENER_STARTED = 'listener started';

export type BroadcastDevice = {
    apiKey: string;
    socketAddress: UdpSocketAddress;
    receivedTimeInMs: number;
};

export type UdpBroadcastStats = {
    sent: number;
    sentFailed: number;
    totalReceived: number;
    pairedReceived: number;
    thisDeviceReceived: number;
};

export type UdpEvent = MessageResult;

export type UdpSocketAddress = {
    address: string;
    port: number;
};

export type UdpMessageExtra = {
    socketAddress: UdpSocketAddress;
};

export enum UdpAction {
    PING = 'udp_paired_device_test_ping',
    PONG = 'udp_paired_device_test_pong',

    BROADCAST_API_KEY = 'udp_broadcast_api_key'
}
